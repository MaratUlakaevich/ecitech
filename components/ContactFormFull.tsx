"use client";

// TODO(cleanup): remove ContactFormFull.tsx and update imports across pages
// to import `ContactForm` directly. ContactForm.tsx is now the single
// unified component; this file is kept only so existing imports of
// `components/ContactFormFull` continue to resolve during the migration.

export { default } from "./ContactForm";
